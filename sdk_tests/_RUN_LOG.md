# SDK Rerun — Run Log

Chronological, append-only. One line per command actually run this session, including
setup/mistakes that don't get a full test entry (per `TEST_LOG_SCHEMA.md`'s logging policy).
This is the running memory of the rerun — the source material for a reproducibility README
once the full rerun is done.

Format: `- [date/time] topic — command — one-line outcome (→ Txx.x if logged as a full test entry)`

---

- [2026-07-13] Setup — renamed target database from `stock-market-decoder` to `stock-decoder` in `.env` (`HYDRA_DB_TENANT_ID`) for a clean rerun tenant.
- [2026-07-13] Topic 1 (Environment setup) — `pip install -r backend/requirements.txt`, `pip install yfinance`, then `python3 -c "import hydra_db, yfinance, fastapi, openai"` — all deps import cleanly, no issues. Not logged as a test entry (no HydraDB behavior involved).
- [2026-07-13] Topic 2 (Create database) — `python3 scripts/setup_and_ingest_sdk.py --step create` against new `stock-decoder` tenant — accepted, database creation started. Deprecation notice on `client.databases.create` reproduced exactly as finding #2 described → T02.1.
- [2026-07-13] Topic 3 (Poll readiness) — `python3 scripts/setup_and_ingest_sdk.py --step poll` — `graph_status=True`, `vectorstore_status.knowledge=True`, `ready_for_ingestion=True` on the very first check, no wait loop needed. Matched expectations cleanly, nothing to log as a finding.
- [2026-07-13] Topic 4 (Prepare source data) — `ls data/*.md | wc -l` confirmed 14 files (13 real + the stale pre-_v2 duplicate from finding #7, kept intentionally — see `test_upsert_claim.py`). `python3 scripts/fetch_price_history.py` — refreshed `data/pton_price_history.csv`, 261 rows. Routine, nothing to log.
- [2026-07-13] Note: `sdk_tests/test_upsert_claim.py` written but NOT yet run — needs to run after topic 5's real ingestion (so `_v2.md` exists as a comparison point), not before.
- [2026-07-13] Topic 5 (Ingest documents) — `python3 scripts/setup_and_ingest_sdk.py --step ingest` — all 13 documents ingested, `success_count=1`/`failed_count=0` each, server-assigned ids matched our supplied ids exactly, no deprecation notice on this call. Clean, matches expected behavior — nothing to log.
- [2026-07-13] Topic 6 (Poll status) — `python3 scripts/setup_and_ingest_sdk.py --step status` — all 13 completed in ~1 minute (vs ~40min/~25min on the original two runs against `stock-market-decoder`). Confirmed via `outputs/_ingestion_results_sdk.json`: zero E6001s or any errors this run. Cancelled a planned controlled cache-hypothesis test (single-file comparison wouldn't be representative of the original batch-entity-resolution scenario) → logged as an observation instead, T06.1. Note: that same results file has a stale `recall_response_sdk` key left over from an earlier session (not new data, script never resets the file between runs).
- [2026-07-13] Cancelled: controlled cache-hypothesis test (sdk_tests/test_ingestion_cache_hypothesis.py) — never actually written to disk; only the empty sdk_tests/artifacts/ scratch dir existed and was removed.
- [2026-07-13] Ran `python3 sdk_tests/test_upsert_claim.py` — confirmed no content-hash dedup (identical content, different filename, got a different auto-assigned id than `_v2.md`'s real id) → T05.1. Script's list()-based verification/cleanup (Part 3/4) failed due to a timing/design flaw on my end (checked immediately post-ingest, relied on title lookup instead of the known id) — fixed the script afterward (waits for indexing status, deletes by known id directly). Leftover test document (id `fb111e7e5c93d37534b06cacedb8fedf`) deleted manually via dashboard by user (delete() itself was never actually invoked in that run — empty loop, not a real failure).
- [2026-07-13] Re-ran fixed `python3 sdk_tests/test_upsert_claim.py` — clean full pass: both ingests reached `completed`, same id both times, `client.context.delete()` succeeded (`deleted_count=1`, confirms finding #11's pattern still holds). Surfaced a new deprecation-notice inconsistency (delete() flags tenant_id/sub_tenant_id as deprecated, ingest() doesn't) → T05.2. Test database left clean, no leftover test document.
- [2026-07-13] Topic 7 (Verify retrieval) — `--step recall --mode fast`: `synthesis_context: None`, matches finding #1 exactly, not logged separately. `--step recall --mode thinking` x4: all 4 runs produced real, grounded, non-stub answers (11 chunks/10 graph_paths every time) — reproducible update to finding #9 for this specific query phrasing → T07.1.
- [2026-07-13] Question raised: could recall be reading cached results from outputs/_ingestion_results_sdk.json instead of live API calls? Checked code (save_results() is write-only, no read-based short-circuit) + wrote sdk_tests/test_recall_not_cached.py — 3 more calls, 3 distinct request_ids, realistic 2.9-3.3s latencies. Confirmed no caching. Folded into T07.1 (now 7/7 total).
- [2026-07-13] Topic 8 (Verify knowledge graph relations) — `cd backend && python ../tests/test_knowledge_graph_relations.py` — finding #14's entity-resolution gap reproduces cleanly on the fresh stock-decoder database: peloton/peloton interactive, inc. and netflix/netflix, inc. both remain unmerged (confirmed via outputs/_knowledge_graph_relations_test_results.json's full_node_registry). Determinism check also clean (same doc queried twice, identical 26 triplets). → T08.1.
- [2026-07-13] Topic 9 (Build event timeline) — `cd backend && python timeline.py` — 42 months built, 5 with events, matching the intended narrative arc exactly (Precor acquisition, Aug 2021, Feb 2022 leadership/restructuring merged correctly under one month, Jun 2022 CFO transition, May 2024 refinancing). Spot-checked outputs/timeline_cache.json directly — all headlines/filing_dates/chunk_counts correct. Clean pass, nothing to log.
- [2026-07-13] Topic 10 (Backend build & run) — `python ../tests/test_orchestrator.py` — deterministic layer OK (5 events loaded correctly). 1/7 classification case FAILed ("what happened after the leadership overhaul?" returned extra ids), and re-ran 2 more times at the user's request — 3/3 consistent fail (not flaky). NOT logged as a HydraDB finding — orchestrator.py never touches HydraDB (pure app-layer code calling OpenAI directly, temperature=0).
- [2026-07-13] Root-caused the above via git archaeology: `git show 147a246:outputs/timeline_cache.json` recovered the ORIGINAL build's 2022-02 headline — "Peloton's leadership overhaul and restructuring plan signal a critical turning point amid financial challenges." — which literally contains the phrase "leadership overhaul", an exact lexical match to the test question. This rerun's LLM-regenerated headline for the same event ("Peloton Interactive undergoes major leadership changes and announces significant restructuring plan amid financial challenges.") drops that phrase entirely. Confirms the fail is caused by run-to-run headline wording variance (timeline.py's headline synthesis is itself LLM-generated, non-deterministic across independent runs), not model drift or a real orchestrator bug. Not a HydraDB issue — timeline.py/synthesis.py are app-layer, no HydraDB call involved in headline generation.
- [2026-07-13] User correction: keep `forbidden={"2022-02"}` (the boundary check itself is still valid and worth asserting) — instead fix the test's question wording to match this run's actual headline language. Changed the case's `"q"` from "What happened after the leadership overhaul?" to "What happened after the leadership changes?" (matching this rerun's regenerated 2022-02 headline, "...undergoes major leadership changes..."), restoring the lexical anchor the classifier relies on, kept `forbidden={"2022-02"}` unchanged. Added a comment noting the question should be re-synced to whatever headline wording timeline.py produces on future reruns. Not logged as a Txx.x test entry — our own test/app-layer fragility, not a HydraDB-attributable finding.
- [2026-07-13] Re-ran `python ../tests/test_orchestrator.py` after the wording fix — 7/7 classification cases PASS, including "What happened after the leadership changes?" (`got=['2022-06','2024-05']`, correctly excludes 2022-02). Deterministic layer also PASS. Confirms the wording-anchor hypothesis was the actual root cause, not model drift. Topic 10's orchestrator test closed out clean; moving to remaining backend test scripts.
- [2026-07-13] `python ../tests/test_price_stats.py` — all sections PASS (boundary-neighbor window derivation, windowed stats for 3 scenarios, too-small-window guard, non-causal framing check). Pure deterministic price math over the already-fetched CSV, no HydraDB/LLM involvement. Nothing to log.
- [2026-07-13] `python ../tests/test_metadata_filter_bucket.py` — re-tested finding #10's metadata_filters shape combinations (top-level plain/list-wrapped, nested under additional_metadata, legacy document_metadata alias, doc_type) — all results match finding #10 exactly, not logged as a repeat. New observation surfaced incidentally in Part 1's raw chunk dump: 2 of 8 chunks (both from peloton_2021-08-26_shareholder-letter_v2.md) carried an undocumented `additional_metadata: {"_retrieval_source": "graph"}` field, never set by our ingestion code, appearing even with `graph_context=False` on the call. Logged as a full entry (T10.1) and added as finding #15 in hydradb_findings_log.md (Category: HydraDB-Docs-mismatch, Severity: Minor).
- [2026-07-13] User asked whether finding #15 (`_retrieval_source`) was also present in previous runs and, if so, why it wasn't documented before. Checked via `git show e2eb109:outputs/_metadata_filter_diagnostic_results.json` (the original build's version of the same diagnostic file) — confirmed the identical `{"_retrieval_source": "graph"}` tag was already present back then too, on 2 different chunks (Feb 2022 shareholder letter + board press release that time). Checked the original `hydradb_findings_log.md`/`CONTEXT_UPDATES.md` at their last committed version (`ea6824c`, pre-rerun) — neither ever mentioned it. Conclusion: not new behavior, reproduces across two independent builds; it was just never noticed/written up the first time — the diagnostic script's original purpose was narrowly answering "does metadata_filters work" (finding #10), not auditing the full raw chunk dump for anything else. Added this provenance note directly into finding #15 and T10.1 rather than treating it as a separate discovery.
- [2026-07-13] Re-ran `python ../tests/test_metadata_filter_bucket.py` (confirmed) then `python ../tests/test_multivalue_filter.py` — single-date filters work individually (02-05 → 2 chunks, 02-08 → 3 chunks), list of both dates together (`{'filing_date': ['2022-02-05', '2022-02-08']}`) returns 0 chunks. Matches finding #10 exactly (no "any of"/IN semantics for multi-value lists of *different* values) — already handled, not logged as a new entry.
- [2026-07-13] `python ../tests/test_monthly_query.py` (diagnostic-only, no pass/fail assertions — informs timeline.py's query design). Checked the specific case this script exists to probe: March 2021 (zero real filings, the "null month" case) — inspected `outputs/_monthly_query_test_results.json` directly. Both unfiltered variants (C, D) returned all 13 corpus-wide chunks (expected, since there's no filing_date to filter by) and `synthesis_context` came back as a generic query-restating stub ("The query asks for specific disclosures... requiring a search filtered by date"), NOT a fabricated narrative about a March 2021 event. No hallucination risk found — matches finding #9's known stub pattern, not a new issue. A2's list-of-dates filter for Feb 2022 returned 0 chunks, matching finding #10 exactly. Feb 2022's variant A (4 chunks) vs. B (2 chunks) — same `filing_date` filter, only query wording differs — is consistent with finding #12's documented thinking-mode reranking instability near small `max_results`, not new. Nothing new to log; all results already explained by findings #9/#10/#12.
- [2026-07-13] `python ../tests/test_chat.py` — full end-to-end chat pipeline (orchestrator → retrieval → price stats → synthesis), 4/4 cases PASS: single-event (Dec 2020 Precor acquisition), multi-event (CFO change + refinancing), range (volatility before CFO transition), off-topic gate (pizza question correctly returns empty scope). All answers grounded, correctly cited, no fabrication. Clean pass, nothing to log — closes out topic 10's backend test scripts. Next: start uvicorn.
- [2026-07-13] `uvicorn main:app --reload --port 8000` — backend server started successfully. Topic 10 (Backend build & run) fully closed out. Moving to topic 11 (Frontend build & connect).
- [2026-07-13] Topic 11 (Frontend build & connect) — `cd frontend && npm install` completed with no errors.
- [2026-07-13] `npm run dev`, loaded `localhost:5173` in browser — Decoder Terminal UI renders correctly: PTON price chart with 5 timeline event markers (Dec 2020, Aug 2021, Feb 2022, Jun 2022, May 2024), Dec 2020 event card showing the correct headline/summary/8-K source panel, decoder query box with 2 suggested questions, and the graph panel legend. Confirms frontend successfully connects to the backend and renders live data end to end. Clean pass, nothing to log. Topic 11 closed out — moving to topic 12 (end-to-end manual pass).
- [2026-07-13] Topic 12 (end-to-end manual pass) — 3 checks via live UI: (1) clicked Feb 2022 timeline marker — correct headline/sources, matches this rerun's data. (2) Typed "What happened after the leadership changes?" into the live decoder box — answer was correct/grounded, BUT the "how we found this" panel showed `scope: 2022-02, 2022-06, 2024-05` (2022-02 INCLUDED), contradicting the same-day `test_orchestrator.py` run which excluded 2022-02 7/7 for the identical question text. Same temperature=0 classify() call, live chat pipeline vs. test harness, different outcome — direct proof this boundary judgment isn't fully pinned down by temperature=0 alone, even with the headline-wording fix in place. (3) Clicked "peloton interactive, inc." graph node — correct entity card (ORGANIZATION, 31 relationships, sensible relation examples). Not a HydraDB issue — orchestrator.py/OpenAI layer, app-side.
- [2026-07-13] Per user's decision, converted the "leadership changes" case in `tests/test_orchestrator.py` to the reproducibility protocol instead of a single-shot assertion: added `"repro_runs": 5` to the case, and refactored `classification_cases()` with a `_grade_once()` helper so this one case runs classify() 5x and is graded PASS (5/5)/FLAKY (partial)/FAIL (0/5) by rate, instead of failing the whole suite on one run. A FLAKY result no longer fails the overall suite (documented boundary-judgment instability, not a regression); a full FAIL (0/5) still does. All other cases unchanged (still single-shot). Ready to re-run.
- [2026-07-13] Re-ran `python ../tests/test_orchestrator.py` with the repro-protocol case — result: 0/5, i.e. FAIL, not flaky. All 5 runs consistently returned `event_ids=['2022-02','2022-06','2024-05']`, including 2022-02 every time — the opposite of the earlier same-day 7/7 exclusion result on the identical wording, and consistent with the live-UI observation from the manual e2e pass. So over the course of today: 7/7 excluded 2022-02 (first test run, right after the wording fix) → then flipped to consistently including it (live UI call + this 5/5 repro run = 6 consecutive inclusions). Other 6 cases still PASS. This is now a *consistent* result, not noisy flakiness — the reproducibility protocol worked exactly as designed and caught that the classifier's behavior on this one boundary case genuinely shifted, not that it's randomly unstable run-to-run. Confirms this specific boundary judgment isn't reliably pinned down by wording OR by temperature=0 alone — points at underlying gpt-4o-mini sampling/session variance external to our code. Not a HydraDB issue. Flagged to user for a decision on how to handle the test going forward, rather than deciding unilaterally.
- [2026-07-13] User caught the real root cause: both 2022-02 AND 2022-06's `timeline.py`-generated headlines independently contain the phrase "leadership changes" (2022-02: "...undergoes major leadership changes and announces significant restructuring plan..."; 2022-06: "...CFO transition...amid leadership changes."). Confirmed directly against `outputs/timeline_cache.json`. So "the leadership changes" was never a stable anchor — it's genuinely ambiguous between two catalog entries (CEO transition vs. CFO transition, both worded as "leadership changes"), which explains the 7/7-then-0/5 flip far better than plain sampling noise. Fixed by replacing the single ambiguous case with two disambiguated ones, each anchored on wording unique to one headline: "What happened after the restructuring plan?" (unique to 2022-02) and "What happened after the CEO transition?" (unique to 2022-02, since 2022-06 is specifically the CFO transition — different role). Both keep `forbidden={"2022-02"}`, `required={"2024-05"}`, back to normal single-shot assertions. Removed the now-unused `repro_runs`/FLAKY-grading machinery from `classification_cases()` (kept the `_grade_once()` helper since it deduplicated logic cleanly either way) — no case currently needs it, keeping the harness lean per policy. Ready to re-run with 8 total cases.
- [2026-07-13] Both disambiguated cases (re-run) STILL landed on the identical scope `['2022-02','2022-06','2024-05']` — confirmed via live UI screenshots for both questions, plus this matched the earlier 5x repro run. So disambiguating which event the phrase refers to was never the actual fix; both phrasings unambiguously point at 2022-02 and the classifier still includes it. Investigated the real cause by reading the actual synthesized answers side by side: both are 100% factually accurate (every figure fact-checked directly against `peloton_2022-02-08_shareholder-letter.md` and `peloton_2022-02-08_restructuring-pr.md` — net loss $439.4M, EBITDA -$266.5M/-23.5% margin, guidance cut $5.4B→$3.7-3.8B, Connected Fitness Subs 2.77M/+66% YoY, 2,800 workforce reduction, $800M cost savings, $150M capex cut, Peloton Output Park wind-down — all matched exactly, zero fabrication either way). Root cause: Feb 2022's own filing bundles the restructuring/CEO-transition ANNOUNCEMENT together with its quantified financial CONSEQUENCES in the same catalog entry, so "after the restructuring plan" has two equally defensible readings (after the announcement vs. after its consequences materialized) — a genuine ambiguity in the source material itself, not a bug. User's call: don't enforce this as a hard rule/test assertion — forcing one arbitrary reading would penalize equally correct model behavior. Removed `forbidden={"2022-02"}` from both cases (now `forbidden=set()`), replaced with a detailed comment documenting the full investigation history and reasoning for future reference. Not a HydraDB issue — orchestrator.py/OpenAI layer, and specifically judged a non-issue (defensible ambiguity), not a defect. Not logged to hydradb_findings_log.md (app-layer, out of HydraDB testing scope) — captured here and in the test file's comments only.
- [2026-07-13] Re-ran `python ../tests/test_orchestrator.py` after dropping the forbidden assertion — 8/8 PASS, clean, including both previously-contentious cases (which now correctly pass regardless of whether the model includes or excludes 2022-02, since that's no longer asserted). Closes out the whole boundary-ambiguity investigation thread. Topic 12 (end-to-end manual pass) fully closed out: UI verified working end to end (timeline markers, decoder chat, graph panel), one real app-layer edge case investigated to a well-evidenced conclusion (not a HydraDB issue), test suite green. Moving to topic 13 (automated regression suite — full `tests/` run).
- [2026-07-13] Checked all 4 available past project sessions (via session transcripts) for how the full `tests/` regression pass was run previously — none of them ever used a consolidated runner (no pytest, no test-runner script); every script was always run individually, standalone, same pattern used throughout this whole rerun. Confirmed this is the correct/only established way — proceeding topic 13 the same way, one script at a time.
- [2026-07-13] `python ../tests/test_chunk_retrieval_stability.py` — re-tested finding #12 on fresh database/ingestion. Inspected `outputs/_chunk_retrieval_stability_results.json` directly: `thinking_max10` 3/8 present (target relevancy scores swung 0.044–0.885, ~20x spread — matches original's 2/8 pattern), `thinking_max20` 8/8 present with tightly-banded scores (~0.847), `fast_max10` 8/8 present with the *identical* score every run (`1.4847047328948975`). Reproduces finding #12 cleanly, strengthening it as systematic rather than a one-off → T13.1 (Fail, Significant, HydraDB-SDK-bug).
- [2026-07-13] `python ../tests/test_retrieval_determinism.py` — checked results via `outputs/_retrieval_determinism_test_results.json` directly. `2021-08-26`: stable, identical chunks/sources/snippets both runs. `2022-02-05`: NOT stable — run 1 returned only `peloton_2022-02-05_board-pr.md` (1 source), run 2 returned both that AND `peloton_2022-02-05_8k.md` (the actual CEO transition filing — McCarthy replacing Foley — plus 6 relation snippets), with no query/code change between calls. This is finding #12's real production-impact scenario reproducing via the actual `hydradb_client.query()`/`synthesis.py` code path (default `max_results=10`, the known-unstable value), not just the isolated diagnostic → T13.2 (Fail, Significant, HydraDB-SDK-bug). Flagged as worth an audit: any code calling `hydradb_client.query()` without overriding `max_results` inherits this risk; `timeline.py` itself already uses `max_results=20` so it's protected, but this diagnostic script (and any future direct caller) is not.
- [2026-07-13] `python ../tests/test_retrieval.py` — deterministic event_id→filing_date mapping (4/4 cases) and live retrieval (2/2 cases, correct dates queried, real evidence returned) both PASS clean. Nothing new, matches expected behavior throughout — not logged as a finding.
- [2026-07-13] `python ../tests/check_memory_readiness.py` against `stock-decoder` — `graph_status`/`scheduler_status`/`vectorstore_status.knowledge`/`vectorstore_status.memories` all `True`. Confirms Memory infra provisions automatically alongside Knowledge on a freshly-created tenant too, matching the original build's finding exactly. Nothing new — not logged as a finding. This closes out topic 13 (all 11 scripts in `tests/` re-run or re-checked this session: test_orchestrator, test_price_stats, test_metadata_filter_bucket, test_multivalue_filter, test_monthly_query, test_chat, test_knowledge_graph_relations [topic 8], test_chunk_retrieval_stability, test_retrieval_determinism, test_retrieval, check_memory_readiness). New findings from this pass: #15 (undocumented _retrieval_source field) plus T13.1/T13.2 re-confirming finding #12 as systematic, one via the production code path. Moving to topic 14 (finalize findings + reproducibility README).
- [2026-07-13] Topic 14 in progress: paused on a few frontend UI changes before finalizing. Built "Tier 1" of a source-provenance feature — chat's "Sources" list (now collapsed behind a "sources (N) →" toggle, stripped of all debug fields per user's call — scope/reasoning/filings-queried/chunk-counts removed entirely, learner-facing chips only) is clickable and jumps the "01 · SOURCE" panel to the exact cited document (`App.jsx`'s new `viewSource()` handler searches all events' documents, not just the selected one, since chat answers can cite documents from other events). Uses HydraDB's own `chunk.source_title` field for attribution — a real, if simple, use of existing HydraDB data. Both changed files (`App.jsx`, `ChatPanel.jsx`) parse-checked clean via `@babel/parser` in the sandbox (full Vite build still needs to run locally per the platform-binary constraint noted in `CONTEXT_UPDATES.md`).
- [2026-07-13] Investigated "Tier 2" (highlighting the exact passage within the opened document, not just opening it) — read the installed SDK's actual `SearchV2Chunk` type directly (`hydra_db/types/search_v2chunk.py`): confirmed no offset/position/span field exists anywhere on a chunk. Also checked `client.context.inspect()` (fetches raw/inferred source content + a download URL, no per-chunk position data) and `client.query()`'s actual signature (confirmed `query_by`/`alpha`/`operator` are real params, but govern corpus-wide retrieval ranking, not within-document location — re-querying with the chunk's own text as a "phrase" search would just return the same chunk again, no offset gained). Grepped the whole codebase for `query_by`/`alpha`/`operator` — zero usage anywhere in this project, meaning every query this whole rerun (and the original build) has run on whatever HydraDB defaults to for `query_by`, never explicitly exercising text-only/BM25 mode, alpha weighting, or the operator field. Logged the offset/position gap as finding #16 in `hydradb_findings_log.md` (Severity: Moderate, source-code-verified not just doc-read) — genuinely new, not previously documented anywhere. Tier 2 itself not pursued as an app fix (would need our own substring/fuzzy-match code against already-fetched document text, no HydraDB call involved).
- [2026-07-13] Started building Tier 2 (app-layer only, no HydraDB call — per finding #16, there's no offset field to query for). Wrote `backend/highlight.py`: `find_chunk_span(document_text, chunk_text)` tries exact substring match, then a whitespace-tolerant regex match (handles markdown line-wrap/reflow differences without loosening what content is actually required), then a difflib longest-matching-block fuzzy fallback (only returned above a 0.6 score threshold, so a poor match surfaces as "no highlight" rather than a misleading one). Wrote `tests/test_source_highlight.py` (deterministic + live sections, same split as test_orchestrator.py/test_retrieval.py). Sanity-checked all 4 deterministic cases (exact/whitespace-tolerant/fuzzy/no-match) directly in the sandbox before handing off — all 4 passed. The live section (real HydraDB query -> real chunk_content -> matched against the real data/*.md file) needs the actual API key, so it's queued for the user to run locally next, per the established pattern for every other HydraDB-touching script.
- [2026-07-13] Ran `python ../tests/test_source_highlight.py` live — 4/4 deterministic PASS, 12/12 real chunks (across 2 queries/5 documents) matched with method=exact, score=1.0. HydraDB's `chunk_content` is verbatim identical to the source markdown for this corpus/mode — the exact-match fast path covers every real case seen so far; whitespace-tolerant/fuzzy paths remain as validated safety nets for corpora where that might not hold. Moving to wiring `/api/highlight` + frontend (DocumentViewer highlighting).
- [2026-07-13] Wired Tier 2 end to end: `backend/main.py` gained `POST /api/highlight` (`{filename, chunk_text}` -> `{match}`, reuses the existing `SAFE_FILENAME_RE` path-safety check from `/api/documents`). `frontend/src/api.js` gained `fetchHighlight()`. `DocumentViewer.jsx` now accepts an optional `highlightChunkText` prop — when set, fetches the match span and renders the document as 3 pieces (before/highlighted/after), auto-scrolling the highlighted piece into view via a ref; falls back to the original single-block render when no highlight is set. `App.jsx` added `highlightChunkText` state, threaded `viewSource(filename, chunkText)` to set it (clearing on manual doc-tab clicks or month changes, so a stale highlight doesn't leak onto unrelated browsing), and passes it into `DocumentViewer`. `ChatPanel.jsx`'s `uniqueSources()` now carries each source's first `chunk_content` alongside its filename so the click handler has both. Highlights only the first chunk per document for now (a doc cited by several chunks in one answer only highlights one) — noted as a possible future refinement, not pursued now. All 5 changed files (`App.jsx`, `ChatPanel.jsx`, `DocumentViewer.jsx`, `api.js`, `main.py`) parse/compile-checked clean in the sandbox. Queued for the user to run locally (`uvicorn` restart + `npm run dev`) and try clicking a source chip.
- [2026-07-13] User confirmed Tier 1/Tier 2 working live, but noted 8-K chunks span the entire (short) document. Wrote `tests/test_chat_e2e.py` (16 questions through the full `chat.run_chat()` pipeline: short-doc baseline, long-document broad/narrow pairs on both shareholder letters, multi-doc single-event narrowing, cross-event comparative, off-topic, and 5 headline-distance stress questions) — live run: 11/11 graded cases PASS. Chunk-length observation: both broad/narrow pairs on the same long document returned the *identical* max chunk length regardless of question specificity — chunking is fixed at ingestion by document structure, not influenced by query text. Doc-count observation: the 2022-02 event's broad and narrow questions returned the identical 5-document set both times — no narrowing there either. Headline-distance: all 5 matched, more than the documented headline-limitation would predict — traced to the classifier's own background knowledge of well-known public facts (Precor, Rob Barker, Jill Woodworth), not anything in the headline text. Not a HydraDB finding (app-layer classify()/synthesis() behavior).
- [2026-07-13] User flagged a real distinction: `chat_answer.yaml` already carries a "do not use outside knowledge" instruction, which appears to conflict with the classify()-stage background-knowledge finding above. Confirmed via direct file reads: that instruction lives in `chat_answer.yaml` (stage 3, answer synthesis) — `orchestrator.yaml`'s classify prompt (stage 1, routing) had no such restriction at all, a genuinely separate LLM call with its own prompt. Routing benefiting from background knowledge doesn't violate a rule that was never applied to routing.
- [2026-07-13] Per user's direction ("nothing should be left to LLM's knowledge... every answer, routing, etc. should be grounded"), read `timeline_event.yaml` (this project's strictest existing prompt) and the user's capstone project's prompts (`supervisor.yaml`, `synthesis.yaml`, `safety_check.yaml`, `entity_extractor.yaml`, `entity_normalizer.yaml`) for reference on grounding-rule strictness. First rewrite drafted RULES/FORMATTING sections for `orchestrator.yaml` and `chat_answer.yaml` — user caught that the draft had become too specific/hardcoded (dataset-instance examples baked into what should be general-purpose prompts, e.g. citing real event ids/headline text as worked examples). Re-analyzed all 8 reference prompts specifically for the grounding/formatting/hardcoding balance: grounding rules are always stated as categories, never instances; formatting rules describe schema/shape, never content; the one place examples appear, they illustrate a transferable pattern (abbreviation expansion, category shape), never a real dataset-specific fact. Redrafted both prompts accordingly, removing all Peloton-specific worked examples (including one pre-existing in the original `orchestrator.yaml`'s BOUNDARY RULE, not something introduced this session).
- [2026-07-13] Wrote and ran `python ../tests/test_orchestrator.py` + `python ../tests/test_prompt_grounding.py` (new — isolated classify()-alone and synthesize_answer()-alone grounding checks) against the rewritten prompts. Found one real regression: the CFO-transition boundary case failed (missing required `2020-12`, forbidden `2022-06` present) — traced to having deleted the BOUNDARY RULE's concrete worked example as part of de-hardcoding; that example had been quietly functioning as a few-shot anchor for the model's date arithmetic. Fixed by replacing it with an abstract, placeholder-based step-by-step procedure (find the anchor's date, compare every other id against it, apply strictly-earlier/later, never include the anchor) instead of a real dataset instance — re-ran clean, 8/8. `test_prompt_grounding.py`: Section A (classify() alone on 5 headline-distance questions) showed the new grounding instruction barely changed behavior — 4/5 still routed via background knowledge, only the purest named-entity case flipped to empty — a real finding that a prompt-level "don't use outside knowledge" instruction only partially suppresses an LLM's use of background knowledge for well-known public entities. Section B (`synthesize_answer()` alone, fabricated chunk containing a checkable fact but omitting a real well-known fact) — clean 3/3 pass: positive control, partial answer-what-you-can-refuse-the-rest, and full refusal all behaved correctly. Not HydraDB findings — orchestrator.py/synthesis.py prompt design, app-layer.
- [2026-07-13] Asked whether HydraDB's own cookbooks/docs define prompt or orchestrator-agent patterns to compare against. Fetched `docs.hydradb.com/essentials/v2/api-results.md` ("How to Use API Results") — HydraDB's own recommended synthesis system prompt is much looser than ours ("answer only from the provided context... say you don't know"), no formatting/category-specificity rules. No "orchestrator" prompt concept exists in their docs at all — the closest analog, the "AI Chief of Staff - Function Routing" cookbook, does routing via retrieval (register candidates as knowledge objects, call `query()`, read off the top match) rather than a bespoke classification LLM call. Also surfaced one concrete divergence worth a future look: HydraDB's docs explicitly warn about a "common mistake" (using `chunk_relations` without `chunk_id_to_group_ids` to attach the right relation to the right chunk) that `synthesis.get_context_snippets()` doesn't follow — flagged, not pursued further this session.
- [2026-07-13] User asked whether the function-routing-via-retrieval pattern could replace `orchestrator.classify()`. Reasoned through it first (not logged as a test) — flagged that `orchestrator.py`'s own docstring already explains why routing was kept out of HydraDB (finding #12's instability), and that boundary-exclusion logic isn't something similarity ranking can do. User asked to test it anyway, since "if it doesn't work well, we have our prompt approach as fallback."
- [2026-07-13] Wrote and ran `tests/test_retrieval_based_routing.py` — ingested the 5 catalog entries as one-line "event card" documents into an isolated collection (`event_router_experiment`, separate from `default`), routed 8 representative questions via `query()` ranking alone, no LLM. Results: clean on single/multi/comparative (real score margins, e.g. 0.76 vs 0.50 for the December question). Boundary case failed exactly as predicted — the anchor event (2022-06) scored *highest*, not excluded, since the question's own words are most similar to its own card. Off-topic (pizza) showed a distinctly different signature: all 5 cards scored within 0.009 of each other, tighter and lower than any real question's spread — a usable but hand-tuned heuristic, not a clean rejection. Headline-distance: the Precor/gyms-hotels question matched cleanly (0.61 vs 0.27) purely via embedding similarity despite zero lexical overlap with the headline — meaning the embedding model itself carries background/world-knowledge associations, same as the LLM classifier did. The Rob Barker and capex questions both failed to route correctly against the headline-only cards. Not a HydraDB finding — expected behavior of similarity-based retrieval, not a bug or claimed capability gap.
- [2026-07-13] User corrected two premises: (1) retrieval was only tested against synthetic one-line headline cards, not the real 13-document corpus HydraDB actually has — real documents contain far more detail (names, numbers) than a headline; (2) boundary-exclusion and off-topic rejection aren't hard requirements for the orchestrator anymore, since `chat_answer.yaml`'s synthesis-stage grounding rule already handles "not enough info here" gracefully — over-retrieval is fine, the real risk is under-retrieval. Only the purely-numeric-question failure was treated as a genuine gap, and only for that gap was a hybrid (retrieval + LLM reasoning) proposed.
- [2026-07-13] Wrote `backend/prompts/retrieval_router.yaml` (new prompt: given the question + REAL retrieved excerpts grouped by event, confirm event_ids + query_type — grounding rule stated as a category same as the other rewritten prompts, "err toward inclusion" explicitly permitted per the relaxed criteria) and `tests/test_retrieval_llm_hybrid.py` (one unscoped `query()` per question across the real `default` collection, `max_results=20`, chunks mapped to event_id via the real ingest manifest's `filing_date`, top-2 excerpts per event fed to the new prompt). Live run: 7/8 pass — both previously-failing headline-distance cases (Rob Barker, $150M capex) now succeed against real excerpt text, confirming those were proxy-corpus artifacts, not retrieval limitations. Only fail: the boundary case, which now shows a genuine recall miss (missing `2020-12` entirely, not just extra inclusion) — removing boundary logic let the model's own reading narrow to the events adjacent to the anchor instead of the true earlier window. Noted every question's retrieved candidate list was all 5 events every time at this corpus's 13-document scale — real discrimination happens entirely in the LLM step reading excerpts, not in retrieval's own filtering.
- [2026-07-13] Added 3 more cases to `test_retrieval_llm_hybrid.py` to test whether recall is capped at 2 events (every prior passing case happened to need exactly 1 or 2): an explicit 3-item multi question, a 3-way comparative, and the "whole story" 5-event case. Live run: all 3 passed cleanly (correct 3, 3, and 5 event_ids respectively) — confirms no cap exists anywhere in the code or prompt; the count simply tracks however many distinct things a question names. Final tally across 11 cases: 10/11 pass, only the known boundary gap failing.
- [2026-07-13] Decision: adopt the hybrid retrieval + LLM-confirmation approach (`backend/prompts/retrieval_router.yaml`) as this project's orchestrator, superseding headline-only `classify()` — over-inclusion is absorbed by `chat_answer.yaml`'s synthesis-stage grounding rule, and the one known gap (boundary/range under-inclusion) is accepted as documented, not fixed. Full write-up in `docs/CONTEXT_UPDATES.md`'s new "Orchestrator redesign" section. Not logged to `hydradb_findings_log.md`/`_index.csv` — app-layer routing/prompt design, not a HydraDB-attributable finding, consistent with this project's logging policy.
- [2026-07-13] Wired the decision in: new `backend/retrieval_router.py` (`route_via_retrieval()`, same return shape as `classify()` so downstream code is unchanged), new `hydradb_client.filename_to_filing_date()` (live `context.list()` lookup, mirrors `source_id_map()`'s pattern, avoids a runtime dependency on `scripts/setup_and_ingest_sdk.py`'s static manifest). `backend/chat.py`'s stage 1 now calls `retrieval_router.route_via_retrieval()` instead of `orchestrator.classify()`; `orchestrator.py` untouched, its docstring updated to say plainly it's superseded/kept as fallback reference. All changed files compile clean in the sandbox (no live credentials here — real validation queued for the user, per the established pattern for every HydraDB-touching change this whole project).
- [2026-07-13] User asked for `chat_answer.yaml`'s pre-strictness-rewrite wording (recalled from earlier in the session: it "worked well"). Reverted to that original text, then re-added exactly one line per explicit instruction — a `{company}`/`{industry}`-templated grounding sentence matching `timeline_event.yaml`'s existing style. `synthesize_answer()` didn't previously take company/industry, so added `COMPANY_NAME`/`COMPANY_INDUSTRY` constants to `synthesis.py` (same values `timeline.py` already uses) and passed them into the `.format()` call. Verified the template renders correctly via a local yaml.safe_load + .format() check in the sandbox.
- [2026-07-13] User asked to further split the insufficient-context instruction: "not relevant" for off-topic, "not in the available data" (or similar) for on-topic-but-thin. Added both. Wrote `tests/test_prompt_grounding.py`'s sibling, `tests/test_chat_answer_wording.py` (4 cases: pizza off-topic, a harder wrong-company/Nike off-topic case, an on-topic-but-undisclosed-detail question, and a control) specifically because the two changes (new `retrieval_router` wiring + new prompt wording) interact: `chat.py` short-circuits before synthesis whenever stage 1 returns empty `event_ids`, so the "not relevant" wording could only ever be user-visible if `retrieval_router` incorrectly included an event for an off-topic question.
- [2026-07-13] Ran `python ../tests/test_chat_answer_wording.py` — both off-topic cases (pizza, Nike) correctly short-circuited at stage 1 (`event_ids=[]`, generic warning, "not relevant" wording never reached); the undisclosed-detail case (Peloton's Dec-2020 ad budget) correctly reached synthesis and answered "It's not in the available data."; control case unaffected. Confirmed the "not relevant" branch is dead code under normal operation — removed it from `chat_answer.yaml` per user's call, keeping only the "not in the available data" instruction. Updated `test_chat_answer_wording.py`'s docstring/case notes afterward to describe this as a settled finding (why the wording was removed) rather than an open question.
- [2026-07-13] Ran `python ../tests/test_chat.py` against the newly-wired pipeline — 3/4 PASS, 1 FAIL: "How volatile was the stock leading up to the CFO transition?" (`required={"2022-02"}`, `forbidden={"2022-06"}`, `type=range`) got `type=single, event_ids=['2022-06']` — the same known boundary/range gap already confirmed twice in `tests/test_retrieval_llm_hybrid.py` before adoption, now reproducing through the real wired pipeline. Not new. Synthesis correctly answered "It's not in the available data" rather than fabricating a volatility figure for the wrong scope — the redesign's core safety property held even on the known-gap case. Restructured `test_chat.py`'s `CASES` from tuples to dicts (matching `test_orchestrator.py`'s style) and marked this case `known_gap: True` — printed but not graded strictly, with a comment pointing to `CONTEXT_UPDATES.md`'s write-up, same treatment as `test_orchestrator.py`'s own accepted-ambiguity case. Other 3 cases unaffected.
- [2026-07-13] Audited `tests/test_prompt_grounding.py` for staleness against the final `chat_answer.yaml` wording — found a real bug: Section B's `REFUSAL_MARKERS` substring list (`"doesn't"`, `"not in the context"`, etc.) didn't include the actual current wording, `"not in the available data"` — none of the old markers match it, so the two refusal-expecting cases would have silently reported FAIL even when the model correctly refused. Added `"not in the available data"` and `"available data"` to the marker list. Compiles clean; logic re-verified by hand against the literal model output recorded in an earlier run ("It's not in the available data.").
- [2026-07-13] Refactored `tests/test_retrieval_llm_hybrid.py` to import and call the real `backend/retrieval_router.py` module (`route_via_retrieval()`) directly, removing its own pre-wiring local reimplementation (duplicate OpenAI client, duplicate prompt loading, duplicate filename-to-event mapping sourced from the static `scripts/setup_and_ingest_sdk.py` manifest instead of the live lookup the real module uses) — eliminates a silent-drift risk where a future change to the real module wouldn't be reflected in this test. Dropped the "retrieved candidates" diagnostic printout (would have needed a second live query per case just for display) since this is now a regression check on production code, not fresh exploration. Re-ran: identical 10/11 result to the pre-refactor run — confirms both that wiring introduced no regression and that the refactored test correctly exercises the real module.
- [2026-07-13] Updated `tests/test_chat_e2e.py`'s docstring and Group 6 comment — both referenced `orchestrator.classify()`'s headline-only limitation as if it still described the running pipeline; corrected to describe `retrieval_router.route_via_retrieval()` (the actual current stage 1) and reframed the headline-distance group as an end-to-end confirmatory check, not a fresh test of a limitation that no longer applies to what's wired in. Also added a note to the graded-cases section flagging that `forbidden` assertions predate the redesign's "err toward inclusion" design intent, so a failure there wouldn't automatically mean a regression. Re-ran `python ../tests/test_chat_e2e.py` — 11/11 graded cases PASS, including every case with a `forbidden` assertion (none tripped) — no relaxation needed after all. Headline-distance diagnostic group also improved to 5/5 matches (previously some spillover into extra events under earlier standalone tests). Chunk-length/doc-count observations unchanged from before the redesign (no shrink, same 5-document set for the 2022-02 broad/narrow pair). Closes out this round of orchestrator-redesign test maintenance — full write-up in `docs/CONTEXT_UPDATES.md`'s "Orchestrator redesign" section.
- [2026-07-13] User provided 6 screenshots from live UI testing of 5 suggested prompts. 4/5 behaved exactly as expected (Dec 2020 grounded answer, 2020-vs-2022 comparative answer with price stats + 7 merged sources, off-topic pizza correctly rejected, undisclosed ad-budget correctly answered "not in the available data"). One anomaly: "How much did Peloton cut its planned capital spending by in early 2022?" asked standalone first returned "It's not in the available data" even though the correct document (Restructuring Press Release, Feb 8, 2022 — confirmed earlier by direct read to literally state the $150M figure) was listed as a retrieved source, and the same fact had just been correctly cited moments earlier in the comparative answer. User re-ran the identical question a second time — this time it correctly answered "Peloton reduced its planned capital expenditures in 2022 by approximately $150 million," sources (5) shown, scoped correctly to the Feb 2022 cluster. Same question, same code, two different outcomes on consecutive runs — a live, user-facing repro of finding #12 (thinking-mode chunk/reranking non-determinism near max_results boundaries) via the real production chat.py -> retrieval_router.py -> synthesis.py path, not just the isolated diagnostic scripts. Not a new bug and not an orchestrator-redesign regression — routing correctly scoped both times; the miss was in which chunk within the correctly-scoped documents got surfaced to synthesis. Logged as a fresh production-path repro of finding #12, not a new finding number.
- [2026-07-13] Frontend chart fixes (options 1+3+4 from the graph-issues walkthrough), no HydraDB involvement — pure frontend layout/rendering. `frontend/src/components/PriceChart.jsx`: (1) tooltip flip — tracks the SVG's real rendered height via getBoundingClientRect() on every mousemove, computes how many real px the hovered point sits from the top, and swaps to a `chart-tooltip-below` class (renders below the point) whenever that's under ~72px, instead of always rendering above and risking clipping near the top of the chart. (3) chart now fills its resizable panel — `.term-chart` changed from `flex: 0 0 auto` to `flex: 1 1 auto; min-height: 0`, `.chart-wrap` given the same, `.price-chart` changed from a fixed `height: 220px` to `height: 100%` — dragging the existing heroHeight resizer (ticker/event-header stay flex:0 0 auto, unchanged) now actually grows/shrinks the chart itself instead of leaving it pinned at a fixed pixel height while only the space below it changed. (4) axis labels moved out of the SVG's `<text>` elements (which were getting non-uniformly squashed by the fixed 900x450 viewBox's preserveAspectRatio="none" stretch) into a `.chart-axis-labels` HTML overlay positioned at the same %-based coordinates already used for the tooltip — real, undistorted text regardless of the chart's own aspect ratio. Removed the now-unused `.axis-label` SVG-text CSS rule (grepped for stray references first, none found). All changes parse-clean via `@babel/parser` in the sandbox (JSX) and brace-balance-checked (CSS); queued for the user to verify visually via `npm run dev` locally, per the established pattern for frontend-only changes needing real-browser confirmation. The separate observation (outer heroHeight resizer's hover highlight not appearing, only the inner graph-panel one responding) was raised but not yet fixed — code inspection found no CSS/JS difference between the two Resizer instances, so root cause is still unconfirmed; queued as a follow-up pending the user's diagnostic check.
- [2026-07-13] User confirmed the chart fixes live: axis labels (y-axis prices, x-axis months) render crisp/undistorted now, chart fills more of the taller panel, and hovering the Dec 2020 peak correctly flips the tooltip below the point instead of clipping at the top. Closes out this round of frontend chart work.
- [2026-07-13] Wrote `tests/test_chat_answer_stability.py` at user's request, to empirically measure finding #12 through the real production pipeline (chat.run_chat()) rather than an isolated SDK diagnostic, after the live UI capex-question anomaly. Grepped outputs/timeline_cache.json's chunk_id evidence directly to confirm which documents are genuinely multi-chunk (not just long by word count): only peloton_2021-08-26_shareholder-letter_v2.md (4 chunks) and peloton_2022-02-08_shareholder-letter.md (5 chunks) qualify — everything else is 1-2 chunks, effectively whole-document. Read peloton_2022-02-08_restructuring-pr.md directly and confirmed its $150M capex fact sits in chunk 0 of 2 (near the top), meaning its earlier live miss was corpus-wide top-K exclusion, not within-document chunk selection — a useful contrast case. 5 questions, 5 runs each (25 total chat.run_chat() calls): 4 target facts confirmed to live in the late chunks (chunk 4 of 5, chunks 2-3 of 4) of the two genuinely multi-chunk letters, the 5th is the original capex question kept for direct comparison with the live anomaly. Each run graded correct/refused/wrong (refused = safe "not in the available data" miss, wrong = a fabrication risk, distinct failure modes). Compiles clean via py_compile in the sandbox; queued for the user to run locally (no live credentials here), per the established pattern for every chat.run_chat()-touching script this whole project.
- [2026-07-13] Ran `python ../tests/test_chat_answer_stability.py` — 25/25 correct (5/5 on every one of the 5 questions), zero refusals, zero wrong answers, including the exact capex question that failed once live in the UI. Checked why: both HydraDB-touching stages of the real pipeline (`retrieval.py`'s stage-2 scoped retrieval and `retrieval_router.py`'s stage-1 routing query) already call `hydradb_client.query()` with `max_results=20`, and both constants carry an inline comment pointing back to `test_chunk_retrieval_stability.py` — the earlier diagnostic that showed the instability specifically at `max_results=10` (thinking_max10: 3/8 present) but not at `max_results=20` (thinking_max20: 8/8 present). So this result doesn't contradict finding #12; it confirms the app's existing max_results=20 mitigation holds up under real chat.run_chat() calls targeting the deepest chunks of the two genuinely multi-chunk documents in the corpus. Honest caveat, not overclaimed: a clean 25/25 in one batch doesn't prove the live capex-question anomaly can't recur — with LLM-based reranking, a lower-probability instability (e.g. single-digit-percent per call) could easily post a clean run by chance; this test demonstrates the mitigation holds for the common case, not that the underlying non-determinism is eliminated. The anomaly itself remains unreproduced and unexplained beyond finding #12's general mechanism. Closes out this round of stability testing.
- [2026-07-13] Full staleness/completeness audit across all logs and reference docs, at user's request, before starting topic 14. Checked: `_index.csv` (9 rows: T02.1, T05.1, T05.2, T06.1, T07.1, T08.1, T10.1, T13.1, T13.2 — all cross-check cleanly against `_RUN_LOG.md` and their topic `.md` files, correct entry counts in each); `hydradb_findings_log.md` (findings #1-16 present, sequentially numbered, internally consistent, no gaps); the 7 topic files that exist (02/05/06/07/08/10/13) all have real content matching their index-declared entry counts; confirmed the 6 topics with no dedicated file (01/03/04/09/11/12) are correctly file-less per the logging policy (nothing HydraDB-attributable was found in any of them, per `_RUN_LOG.md`'s own lines for those topics) — not a gap. Found and fixed two real issues: (1) `docs/CONTEXT_UPDATES.md`'s "Known limitations" section described chat scoping as "headline-limited" in the present tense, as if `orchestrator.classify()` were still the live stage 1 — stale since the retrieval_router redesign; rewrote it to past tense with a pointer to the "Orchestrator redesign" section, and fixed a quote of its old heading text in `tests/test_chat_e2e.py`'s own docstring to match. (2) `docs/PELOTON_DATASET.md` and (implicitly) `CONTEXT_UPDATES.md` both still framed "Phase 2: price history" and "Phase 3: Wikipedia" as not-yet-finalized/upcoming — Phase 2 was in fact finalized and used throughout (`pton_price_history.csv`, `price_stats.py`), and Phase 3 was never ingested (confirmed via a repo-wide grep for "wikipedia" — zero hits outside planning docs). Per user's explicit call, this is not written up as a documented scope-cut anywhere (future-considerations language will cover "growing the dataset" generically instead) — just cleaned `PELOTON_DATASET.md`'s stale "Not yet finalized"/"Still open before ingestion" sections to reflect the dataset as actually built (13 filings + price history, both finalized), and retitled the doc from "Finalized (Phase 1: SEC 8-Ks)" to plain "Finalized." Also added a closing addendum to `CONTEXT_UPDATES.md`'s "Orchestrator redesign" section documenting the 5 live-UI test prompts, the capex-question anomaly, and `test_chat_answer_stability.py`'s 25/25 result with the max_results=20 explanation — including the explicit, un-softened caveat that a small dataset (13 documents) makes this kind of instability structurally less likely to surface, and a larger corpus with more/longer documents would be expected to make it more evident, not less. All docs now consistent with the actual end-to-end-complete state of the project. Closes out the pre-topic-14 audit.
